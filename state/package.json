{
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "build": "npx ncc build ./src/main.ts --out=./out --minify --target=es2022",
    "start": "npm run build && node ./out/index.js",
    "test": "npx jest",
    "clean": "rm -fr ./out"
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "@types/node": "^20.4.2",
    "@vercel/ncc": "^0.36.1",
    "jest": "^29.6.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.370.0",
    "commander": "^11.0.0"
  }
}
